defmodule TestlyHome.ProjectControllerTest do
  use TestlyHome.ConnCase, async: true

  # test "GET /projects/new", %{conn: conn} do
  #   conn = get(conn, "/projects/new")
  #   assert html_response(conn, 200) =~ "Enter the site you would like to start with"
  # end
end
