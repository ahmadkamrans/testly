import EctoEnum

defenum(Testly.SessionRecordings.ReferrerSourceEnum, :session_recordings_referrer_source_enum, [
  :social,
  :search,
  :paid,
  :email,
  :direct,
  :unknown
])

defenum(Testly.SessionRecordings.DeviceTypeEnum, :session_recordings_device_type_enum, [
  :desktop,
  :mobile,
  :tablet
])
